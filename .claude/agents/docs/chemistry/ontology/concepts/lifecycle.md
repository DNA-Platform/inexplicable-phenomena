---
kind: redirect
title: Lifecycle
status: stable
---

# Lifecycle

The phase ordering: setup, mount, render, layout, effect, unmount. Each phase has an awaitable transition via `next(phase)`.

This page is part of the **ontology spine**.

- The user surface: [lifecycle phases (feature)][feat-lifecycle].
- The book chapter: [`$Particle` book — lifecycle][book-lifecycle].

<!-- citations -->
[feat-lifecycle]: ../../features/lifecycle-phases.md
[book-lifecycle]: ../../books/particle/lifecycle.md
