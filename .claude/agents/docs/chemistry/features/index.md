# Chemistry features

One page per concept on the chemistry surface. See the [L-3 backlog] for the planned set of pages.

Use the [feature template] when adding a new page. Each page follows the same shape: what it is, how it works, caveats (linked, not repeated), related.

<!-- citations -->
[L-3 backlog]: ../../_backlog-l3.md
[feature template]: ../../_template-feature.md
