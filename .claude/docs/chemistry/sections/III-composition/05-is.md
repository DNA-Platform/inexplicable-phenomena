---
kind: catalogue-section
section: III.5
title: $is<T>(ctor)
status: stub
---

# § III.5 `$is<T>(ctor)`

## Definition

`$is<T>(ctor)` is a type-only helper for `$check` (§ III.4) signatures. It produces a TypeScript-level "this argument's type is T" assertion that the parameter-validation machinery can consume.

## Rules

- *(TBD — pure type-level helper, no runtime cost.)*

## Cases

- A `$check(child, $is($Item))` invocation.

## See also

- [§ III.4 `$check`][s-III-4] — what consumes `$is`.

<!-- citations -->
[s-III-4]: ./04-check.md
