---
kind: redirect
title: Render filtering
status: stable
---

# Render filtering

Cross-cutting view interception. A registered filter can rewrite, hide, or replace what a particle renders. Built-in: `$show` / `$hide`. Extension point: `registerFilter(fn)`.

This page is part of the **ontology spine**.

- The user surface: [render filters (feature)][feat-render-filters].
- The book chapter: [`$Particle` book — render filters][book-render-filters].

<!-- citations -->
[feat-render-filters]: ../../features/render-filters.md
[book-render-filters]: ../../books/particle/render-filters.md
