# Arthur's Library

Architect — workspace boundaries, dependency graphs, monorepo structure, ES module semantics, package design.

Books go here as `{slug}.md` files with frontmatter. See [library README](../README.md) for the format.
