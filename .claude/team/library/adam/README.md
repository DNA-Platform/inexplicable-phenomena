# Adam's Library

Automation engineer — relay systems, listener loops, UIA reading, message delivery protocols, cross-process coordination.

Books go here as `{slug}.md` files with frontmatter. See [library README](../README.md) for the format.
