// GENERATED by assembly/routes.ts on every build — never edited. Where each book of the library stands.
import type { $Book } from '@dna-platform/public';
import { books } from './books';

export type Route = { name: string; folder: string; address: string; load: () => Promise<{ book: $Book }> };

export const routes: Route[] = [
    { name: "wikipedia-manual-of-style-layout", folder: ".article", address: "/wikipedia-manual-of-style-layout", load: books[".article"] },
    { name: "wikipedia", folder: ".encyclopedia", address: "/", load: books[".encyclopedia"] },
    { name: "alan-turing", folder: "turing", address: "/alan-turing", load: books["turing"] },
];

export const root: Route | undefined = routes[1];
