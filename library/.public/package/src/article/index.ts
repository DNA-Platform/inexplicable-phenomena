// CREATED 2026-09-08 — Sprint 53 scaffold. THE DOOR `@dna-platform/public/article` (T9/T12: base = writing, reference, formatting as the default door; book, article, encyclopedia their own).
// OWED (U10): a rollup input `article: 'src/article/index.ts'`, a package.json exports entry `./article`, and a `.d.ts` build — the same three lines encyclopedia already has. Not wired until the seam settles, so nothing here is reachable from a consumer yet.
export * from './Article';
export * from './Margin';
export * from './Theorem';
export * from './Theme';
