// GENERATED by assembly/routes.ts on every build — never edited. Where each book of the library stands.
import type { $Book } from '@dna-platform/public';

export type Route = { name: string; address: string; load: () => Promise<{ book: $Book }> };

export const routes: Route[] = [
    { name: "wikipedia-manual-of-style-layout", address: "/wikipedia-manual-of-style-layout", load: () => import("./books/.article") },
    { name: "wikipedia", address: "/", load: () => import("./books/.encyclopedia") },
    { name: "alan-turing", address: "/alan-turing", load: () => import("./books/turing") },
];

export const root: Route | undefined = routes[1];
