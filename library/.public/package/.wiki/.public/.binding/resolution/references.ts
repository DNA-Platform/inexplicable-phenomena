// STUB — Sprint 70 U5. Every reference a chapter writes resolves against the route table, or a
// diagnostic names the file and line. Doug: "vite checking the correctness of the ones that are
// found to exist."
import type { Diagnostic } from '../inventory/library';
import type { Table } from './addresses';

export const references = (_table: Table): Diagnostic[] => [];
