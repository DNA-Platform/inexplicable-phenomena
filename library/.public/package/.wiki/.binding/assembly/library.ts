// STUB — Sprint 70 U6. Writes ../library.tsx composing every book into one $Library, so the
// library is a single constructible object that specification can ask to specify itself and
// an application can draw as a catalogue. Waits on the $Library kind in the package.
import type { Library } from '../inventory/library';

export const compose = (_library: Library): never => {
    throw new Error('not implemented: assembly/library — Sprint 70 U6, waits on $Library');
};
