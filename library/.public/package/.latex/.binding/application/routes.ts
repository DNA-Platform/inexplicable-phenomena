// GENERATED by assembly/routes.ts on every build — never edited. Where each book of the index stands.
import type { $Book } from '@dna-platform/public';
import { books } from './books';

export type Route = { name: string; address: string; load: () => Promise<{ book: $Book }> };

export const routes: Route[] = [
    { name: "aaronson", address: "/", load: books["aaronson"] },
];

export const root: Route | undefined = routes[0];
