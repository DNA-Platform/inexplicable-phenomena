// STUB — Sprint 70 U7. Emits .pubconfig.schema.json from every step's configuration slice,
// so an editor autocompletes and validates .pubconfig while it is typed.
export const schema = (): never => {
    throw new Error('not implemented: configuration/schema — Sprint 70 U7');
};
