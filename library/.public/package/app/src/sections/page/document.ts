export const documentSource = String.raw`# The Algebra of Perspective

Every act of *reading* is a change of coordinates. A page holds one representation; a reader arrives carrying another; **meaning** is what survives the transformation between them.

Mathematics learned this lesson early. The same circle is $x^2 + y^2 = r^2$ to Descartes and $r e^{i\theta}$ to Euler — one object, two notations — and binding every notation to every other is the most beautiful identity in the language:

$$e^{i\pi} + 1 = 0$$

A library is built on the same wager. A book is not its paper but its **structure** — words that point, sentences that contain, titles that stand first and speak for the whole. When a paragraph is parsed into sentences and a sentence into words, nothing is stored twice: every reading is computed fresh from the one source, the way $f(x)$ is computed from $x$.

---

The transform has an inverse. Writing goes down through the same door reading came up through: prose becomes a block, the block becomes parts, and the parts — *words, formulas, emphasis* — remain live objects a program can count, question, and recompose. The Fourier pair says it exactly:

$$\hat{f}(\xi) = \int_{-\infty}^{\infty} f(x)\, e^{-2\pi i x \xi}\, dx$$

What you are reading is not converted text. It is a **composition** — paragraphs holding sentences holding words, with mathematics standing inline like any other citizen — rendered by the same objects that can tell you, in the footer below, exactly how many words this page holds.`;
